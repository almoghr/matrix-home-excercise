 const calculationMethods = ["+", "-", "*", "/"];
 const ACCESS_TOKEN_LIFE_IN_MS = 1000 * 60 * 5;
 const COOKIE_MAX_AGE_LIFE_IN_SECONDS = 60 * 60 * 24 * 30;

 module.exports = {
    calculationMethods,
    ACCESS_TOKEN_LIFE_IN_MS,
    COOKIE_MAX_AGE_LIFE_IN_SECONDS
 }
